export const getContacts = state => state.phones.phones;

export const getLoading = state => state.phones.isLoading;

export const getError = state => state.phones.error;

export const getFilter = state => state.filter;
